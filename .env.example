# Jumbo API Environment Variables
# Copy this file to .env and fill in your credentials
# NEVER commit .env to git!

# Jumbo.com login credentials (optional)
# If set, the API will automatically login on startup
# and re-authenticate when the session expires
JUMBO_USERNAME=your-email@example.com
JUMBO_PASSWORD=your-password

# Timezone (optional, default: Europe/Amsterdam)
TZ=Europe/Amsterdam
